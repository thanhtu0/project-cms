import { Card, OrderDetailList } from '~/layouts/components';

const Home = () => {
    return (
        <div>
            <Card />
            <OrderDetailList />
        </div>
    );
};

export default Home;
